<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="ToolbarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlHighlight">@color/control_highlight_color</item>
        <item name="colorControlNormal">@color/control_normal_color</item>
        <item name="android:textColorPrimary">@color/control_normal_color</item>
        <item name="android:textColorSecondary">@color/control_normal_color</item>
        <item name="android:textColor">@color/control_normal_color</item>
    </style>

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="PopupDialogTheme" parent="ThemeOverlay.AppCompat.Dialog">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="PopupWindow" parent="ThemeOverlay.AppCompat.Dialog">
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <!--<item name="android:windowCloseOnTouchOutside">true</item>-->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>